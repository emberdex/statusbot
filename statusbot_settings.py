# The host on which the database is running.
database_host = "localhost"
# The port on which the database is running.
database_port = 27017
# The name of the database.
database_name = "issues"
# The collection within the database.
database_collection = "issues"
# The host on which the app should bind.
# 127.0.0.1 binds to localhost, 0.0.0.0 binds to all interfaces, and you can also enter a specific IP.
app_host = "0.0.0.0"
# The port on which the app should bind.
# You may want to change this for development purposes.
app_port = 80
# The name of the app.
# Shows up in the navbar, as well as on the home page.
app_name = "StatusBot"
# The app's slogan.
# Shows up on the home page beneath the app_name.
app_slogan = "The easiest way to check if it's just you."
